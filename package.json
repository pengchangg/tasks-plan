{
  "name": "growjoy-h5",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "go run ./cmd/growjoy serve --demo",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "test:go": "go test ./...",
    "check:ui": "npm run build && node scripts/with-service.mjs scripts/visual-check.mjs",
    "check:flow": "npm run build && node scripts/with-service.mjs scripts/flow-check.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "playwright": "^1.63.0",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
